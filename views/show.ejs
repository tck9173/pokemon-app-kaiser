<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" href="/css/app.css">
    </head>
    <body>
        <h1>Pokemon</h1>
        <div class = "content">
        <a href="/pokemon/">Main Page</a></br>
        <%=pokemon.name%> </br>
        <div class="pokemonShow">
            <img class='fullsize' src= '<%= pokemon.img%>'>
            <div class= "caughtBy">
                <h4>Caught by:</h4>
                <ul>
                <% for (let i=0; i<pokemon.Players.length; i++) {%>
                    <li>
                        <%=pokemon.Players[i].name%>
                    </li>
                <% } %>
                </ul>
            </div>
        </div>
        <a href="/pokemon/<%=pokemon.id%>/edit">   Edit</a>
        <form action='/pokemon/<%=pokemon.id%>?_method=DELETE' method='POST'>
            <input type='submit' value='DELETE'/>
        </form>
        </div>
    </body>
</html>